services:
  db:
    image: postgres:16-alpine
    container_name: kehadiran-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    # Dibuka pada localhost server sahaja (untuk pgAdmin/psql). Tidak terdedah ke internet.
    # ports:
    #   - "127.0.0.1:${DB_PORT_HOST:-5432}:5432"
    volumes:
      - kehadiran_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 12

  app:
    build: .
    container_name: kehadiran-app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      APP_PORT: ${APP_PORT:-3000}
      # Sambungan dalaman ke servis db (bukan localhost)
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      RUN_MIGRATIONS_ON_START: ${RUN_MIGRATIONS_ON_START:-true}
      RUN_SEED_ON_START: ${RUN_SEED_ON_START:-true}
      TIMEZONE: ${TIMEZONE:-Asia/Kuala_Lumpur}
      # Sesi & seed pengguna (Fasa 8)
      SESSION_SECRET: ${SESSION_SECRET:-}
      SESSION_MAX_AGE_MS: ${SESSION_MAX_AGE_MS:-28800000}
      SEED_SUPERADMIN_USERNAME: ${SEED_SUPERADMIN_USERNAME:-superadmin}
      SEED_SUPERADMIN_PASSWORD: ${SEED_SUPERADMIN_PASSWORD:-}
      SEED_SUPERADMIN_NAMA: ${SEED_SUPERADMIN_NAMA:-SU HEM}
      SEED_ADMIN_USERNAME: ${SEED_ADMIN_USERNAME:-admin}
      SEED_ADMIN_PASSWORD: ${SEED_ADMIN_PASSWORD:-}
      SEED_ADMIN_NAMA: ${SEED_ADMIN_NAMA:-SU Kehadiran}
      # Google Sheets (Fasa 2) — READ-ONLY
      GOOGLE_APPLICATION_CREDENTIALS: ${GOOGLE_APPLICATION_CREDENTIALS:-secrets/service-account.json}
      SHEET_MASTER_PELAJAR_ID: ${SHEET_MASTER_PELAJAR_ID:-}
      SHEET_KEHADIRAN_ID: ${SHEET_KEHADIRAN_ID:-}
    ports:
      - "3010:3000"
    volumes:
      # Fail kunci service account (Fasa 2) — read-only
      - ./secrets:/app/secrets:ro

volumes:
  kehadiran_pgdata:
